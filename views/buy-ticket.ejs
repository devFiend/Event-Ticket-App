<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Ticket</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include ('partials/header') %>
    <div class="container">
        <h1>🎟️ <%= event.title %></h1>
        <img src="<%= event.imageUrl %>" alt="<%= event.title %>" class="event-image">

        <div class="event-details">
            <p><strong>📅 Date:</strong> <%= event.date %></p>
            <p><strong>💲 Price:</strong> $<%= event.price %></p>
            <p class="description"><%= event.description %></p>
        </div>

        <form action="/mock-payment/<%= event._id %>" method="POST" class="ticket-form">
            <input type="text" name="buyerName" placeholder="👤 Your Name" required>
            <input type="email" name="buyerEmail" placeholder="📧 Your Email" required>
            <button type="submit" class="btn-primary">Proceed to Payment</button>
        </form>

        <a href="/" class="back-link">⬅ Back to Events</a>
    </div>
</body>
</html>
